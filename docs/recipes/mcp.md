<!-- GENERATED by scripts/gen_recipes.mjs from spec/recipes.json. Do not edit by hand. -->

# Viktor over MCP: one recipe per framework and client

Viktor hosts a stateless Streamable-HTTP MCP server at `https://api.viktor.com/mcp`. Authenticate with a Viktor
API key as `Authorization: Bearer <key>` (`x-api-key` also works). The tool list depends on the key's scopes;
`ask_viktor` needs `threads:create`, `runs:create` and `runs:read`. A personal key needs a linked Slack or
Teams identity. Waits are capped at 300 s server-side: for longer work call `wait_for_run` again, or use the
`delegate_to_viktor` tool from a Viktor adapter package. Set client read timeouts above 300 s.

Native packages exist for the Vercel AI SDK, LangChain, Pydantic AI, Mastra and the OpenAI Agents SDK; use those
when you want Viktor as a *model* or a typed delegate tool. MCP is the zero-install path everywhere else.

### OpenAI Agents SDK (Python)

```python
from agents.mcp import MCPServerStreamableHttp

viktor = MCPServerStreamableHttp(
    name="viktor",
    params={"url": "https://api.viktor.com/mcp", "headers": {"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"}, "timeout": 30},
    client_session_timeout_seconds=330,
)
agent = Agent(name="Assistant", mcp_servers=[viktor])
```

<sub>Checked: docs 2026-09-20</sub>

### OpenAI Agents SDK (JS)

```ts
import { MCPServerStreamableHttp } from "@openai/agents";

const viktor = new MCPServerStreamableHttp({
  name: "viktor",
  url: "https://api.viktor.com/mcp",
  requestInit: { headers: { Authorization: `Bearer ${process.env.VIKTOR_API_KEY}` } },
});
```

<sub>Checked: docs 2026-09-20</sub>

### Vercel AI SDK

```ts
import { createMCPClient } from "@ai-sdk/mcp";

const viktor = await createMCPClient({
  transport: { type: "http", url: "https://api.viktor.com/mcp", headers: { Authorization: `Bearer ${process.env.VIKTOR_API_KEY}` } },
});
const tools = await viktor.tools();
```

<sub>Checked: docs 2026-09-20</sub>

### Mastra

```ts
import { MCPClient } from "@mastra/mcp";

const mcp = new MCPClient({ servers: { viktor: {
  url: new URL("https://api.viktor.com/mcp"),
  requestInit: { headers: { Authorization: `Bearer ${process.env.VIKTOR_API_KEY}` } },
} } });
const tools = await mcp.getTools();
```

<sub>Checked: docs 2026-09-20</sub>

### LangChain / LangGraph (Python)

```python
from langchain_mcp_adapters.client import MultiServerMCPClient

client = MultiServerMCPClient({"viktor": {
    "transport": "http",
    "url": "https://api.viktor.com/mcp",
    "headers": {"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"},
}})
tools = await client.get_tools()
```

<sub>Checked: docs 2026-09-20</sub>

### LangChain / LangGraph (JS)

```ts
import { MultiServerMCPClient } from "@langchain/mcp-adapters";

const client = new MultiServerMCPClient({ viktor: {
  url: "https://api.viktor.com/mcp",
  headers: { Authorization: `Bearer ${process.env.VIKTOR_API_KEY}` },
} });
const tools = await client.getTools();
```

<sub>Checked: docs 2026-09-20</sub>

### Pydantic AI

```python
from pydantic_ai import Agent
from pydantic_ai.mcp import MCPToolset

viktor = MCPToolset("https://api.viktor.com/mcp", headers={"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"})
agent = Agent("openai:gpt-5.2", toolsets=[viktor])
```

<sub>Checked: docs 2026-09-20</sub>

### Google ADK (Python)

```python
from google.adk.tools.mcp_tool import McpToolset, StreamableHTTPConnectionParams

viktor = McpToolset(connection_params=StreamableHTTPConnectionParams(
    url="https://api.viktor.com/mcp",
    headers={"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"},
    timeout=30, sse_read_timeout=330,
))
```

<sub>Checked: docs 2026-09-20</sub>

### CrewAI

```python
from crewai import Agent
from crewai.mcp import MCPServerHTTP

agent = Agent(role="Ops", goal="…", backstory="…", mcps=[MCPServerHTTP(
    url="https://api.viktor.com/mcp", headers={"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"}, streamable=True,
)])
```

<sub>Checked: docs 2026-09-20</sub>

### LlamaIndex

```python
from llama_index.tools.mcp import BasicMCPClient, McpToolSpec

client = BasicMCPClient("https://api.viktor.com/mcp", headers={"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"})
tools = await McpToolSpec(client=client).to_tool_list_async()
```

<sub>Checked: docs 2026-09-20</sub>

### Haystack

```python
from haystack.utils import Secret
from haystack_integrations.tools.mcp import MCPToolset, StreamableHttpServerInfo

viktor = MCPToolset(server_info=StreamableHttpServerInfo(url="https://api.viktor.com/mcp", token=Secret.from_env_var("VIKTOR_API_KEY")))
```

<sub>Checked: source 2026-09-20</sub>

### Microsoft Agent Framework (Python)

```python
from agent_framework import MCPStreamableHTTPTool

viktor = MCPStreamableHTTPTool(
    name="viktor", url="https://api.viktor.com/mcp",
    header_provider=lambda _kwargs: {"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"},
)
```

<sub>Checked: docs 2026-09-20</sub>

### Semantic Kernel (Python)

```python
from semantic_kernel.connectors.mcp import MCPStreamableHttpPlugin

viktor = MCPStreamableHttpPlugin(name="viktor", url="https://api.viktor.com/mcp",
    headers={"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"})
```

<sub>Checked: source 2026-09-20</sub>

### AutoGen 0.4+

```python
from autogen_ext.tools.mcp import StreamableHttpServerParams, mcp_server_tools

tools = await mcp_server_tools(StreamableHttpServerParams(
    url="https://api.viktor.com/mcp", headers={"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"}, sse_read_timeout=330))
```

<sub>Checked: docs 2026-09-20</sub>

### AG2

```python
from ag2.tools import MCPToolkit
from ag2.tools.toolkits.mcp_server.types import MCPServerConfig

viktor = MCPToolkit(MCPServerConfig(server_url="https://api.viktor.com/mcp", authorization_token=os.environ["VIKTOR_API_KEY"]))
```

<sub>Checked: source 2026-09-20</sub>

### smolagents

```python
from smolagents import MCPClient

with MCPClient({"url": "https://api.viktor.com/mcp", "transport": "streamable-http",
                "headers": {"Authorization": f"Bearer {os.environ['VIKTOR_API_KEY']}"}}) as tools:
    ...
```

<sub>Checked: source 2026-09-20; the headers key is undocumented upstream and needs mcp SDK 1.x</sub>

### Claude Code

```bash
claude mcp add --transport http viktor https://api.viktor.com/mcp --header "Authorization: Bearer $VIKTOR_API_KEY"
```

<sub>Checked: docs 2026-09-20</sub>

### Cursor (`~/.cursor/mcp.json`)

```json
{ "mcpServers": { "viktor": { "url": "https://api.viktor.com/mcp", "headers": { "Authorization": "Bearer ${env:VIKTOR_API_KEY}" } } } }
```

<sub>Checked: docs 2026-09-20</sub>

### VS Code (`.vscode/mcp.json`)

```json
{ "inputs": [{ "type": "promptString", "id": "viktor-key", "description": "Viktor API key", "password": true }],
  "servers": { "viktor": { "type": "http", "url": "https://api.viktor.com/mcp", "headers": { "Authorization": "Bearer ${input:viktor-key}" } } } }
```

<sub>Checked: docs 2026-09-20</sub>

### Codex CLI

```bash
codex mcp add viktor --url https://api.viktor.com/mcp --bearer-token-env-var VIKTOR_API_KEY
```

<sub>Checked: docs 2026-09-20</sub>

### Gemini CLI

```bash
gemini mcp add --transport http --header "Authorization: Bearer $VIKTOR_API_KEY" viktor https://api.viktor.com/mcp
```

<sub>Checked: docs 2026-09-20</sub>

### Windsurf (`mcp_config.json`)

```json
{ "mcpServers": { "viktor": { "serverUrl": "https://api.viktor.com/mcp", "headers": { "Authorization": "Bearer ${env:VIKTOR_API_KEY}" } } } }
```

<sub>Checked: docs 2026-09-20</sub>

### Zed (`settings.json`)

```json
{ "context_servers": { "viktor": { "url": "https://api.viktor.com/mcp", "headers": { "Authorization": "Bearer <your key>" } } } }
```

<sub>Checked: docs 2026-09-20</sub>

### Cline (`cline_mcp_settings.json`)

```json
{ "mcpServers": { "viktor": { "type": "streamableHttp", "url": "https://api.viktor.com/mcp", "headers": { "Authorization": "Bearer <your key>" } } } }
```

<sub>Checked: docs 2026-09-20</sub>

### Claude Desktop and other stdio-only clients (bridge)

```json
{ "mcpServers": { "viktor": { "command": "npx", "args": ["-y", "viktor-mcp"], "env": { "VIKTOR_API_KEY": "<your key>" } } } }
```

<sub>Checked: bridge tests 2026-09-20</sub>

## Not supported yet

claude.ai / Claude Desktop connectors and ChatGPT connectors require OAuth on the MCP server; Viktor's server
authenticates with API keys today. Claude Desktop works through the stdio bridge above.
